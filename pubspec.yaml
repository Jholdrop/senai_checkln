name: exemplo_senai_checkln
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  geolocator: ^14.0.2
  image_picker: ^1.2.0
  path: ^1.9.1
  permission_handler: ^12.0.1
  sqflite: ^2.4.2+1
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
